import { DisplayNoneDirective } from './display-none.directive';

describe('DisplayNoneDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayNoneDirective();
    expect(directive).toBeTruthy();
  });
});
