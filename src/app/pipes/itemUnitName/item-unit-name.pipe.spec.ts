import { ItemUnitNamePipe } from './item-unit-name.pipe';

describe('ItemUnitNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemUnitNamePipe();
    expect(pipe).toBeTruthy();
  });
});
